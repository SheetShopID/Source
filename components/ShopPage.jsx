export default function ShopPage({ shop }) {
  return (
    <div style={{ padding: 20 }}>
      <h1>Toko: {shop}</h1>
      <p>Subdomain ini menampilkan data toko dari Firebase.</p>
    </div>
  );
}
